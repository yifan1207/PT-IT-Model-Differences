# Exp23 Position-Sensitivity Synthesis

This paper-facing note is generated by `scripts/analysis/analyze_first_divergence_position_sensitivity.py` from stored Exp23 raw-shared first-divergence records.

The primary Exp23 first-divergence pool has `2983` valid dense-family events, mean generated position `2.17`, median `0`, `1499` at position `0`, and `495` at position `>=5`.

After dropping position-0 records, the common-IT upstream-late interaction is `2.25` logits (95% CI `[2.11, 2.38]`).
Restricting to position `>=3`, it is `1.52` logits (95% CI `[1.36, 1.68]`), and Gemma-removed is `0.79` logits (95% CI `[0.70, 0.88]`); all five family-specific intervals are above zero, with Llama close to the boundary (`0.01`).
Restricting to position `>=5`, it is `1.64` logits (95% CI `[1.39, 1.88]`), and Gemma-removed is `0.83` logits (95% CI `[0.71, 0.94]`).
At `>=5`, per-family support is thinner: Llama is `0.12` logits (95% CI `[-0.05, 0.27]`).

Interpretation: first-divergence position affects magnitude and family-level power, but the pooled interaction is not first-token-only.
