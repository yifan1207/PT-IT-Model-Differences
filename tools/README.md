# Tools

Small local utilities and one-off helpers that are useful during analysis or repo maintenance, but are not part of the core experiment pipeline.

Current contents include:

- `test_audit.py`: standalone audit-style checks for shared collection code.
- `tmp_rerun_e310.py`: one-off helper for rerendering the E3.10 plot.
